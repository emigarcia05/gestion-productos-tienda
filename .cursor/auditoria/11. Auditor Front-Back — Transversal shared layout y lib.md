Lee **obligatorio** con la herramienta Read (no asumas de memoria):

1. `.cursor/auditoria/_CONTRATO.md`
2. Las secciones de docs de **Alcance** (abajo). No leas las guías enteras.

Ejecutá las **4 tareas del contrato** (muerto → modularizar → eficientizar → docs del lote) **solo** sobre este lote. No pases al lote 12.

Este chat es el **lote 11 de 12**.

---

## Alcance

**Dominio transversal:** layout (sidebar, dock, áreas), sesión/auth, shared UI, hooks, lib de validación/gates, usuarios, APIs genéricas (indicador slidenav, sync DUX **orquestación** no dominio).

**Guías (solo estos §):**

- FRONTEND_GUIDELINES **§1** (patrones, sidebar, URLs) + **§2** catálogo + Checklist **§4**
- BACKEND_GUIDELINES **§1** (Actions, seguridad, TZ, Prisma, ActionResult) + **§2** patrones + **§3.1** `global_personal` / usuarios + **§6** herramientas
- No reabrir dominios 1–10 salvo import roto por un borrado

**App / UI / lib**

- `src/components/layout/`, `src/components/shared/`, `src/components/ui/` (no reescribir shadcn; sí muerto/duplicado nuestro)
- `src/app/finanzas/usuarios/`
- `src/app/page.tsx`, hubs `/finanzas`, `/marketing` (hubs vacíos)
- `src/lib/` (sesión, `actionGates`, `apiRouteAuth`, `fechaArgentina`, `ui-classes`, `hooks/`, `validations/common`)
- `src/actions/sesion.ts`, `globalPersonal.ts`
- `src/app/api/indicador-slidenav/`, sync DUX **router** genérico si no pertenece a un lote anterior
- `src/middleware.ts` — no cambiar política; sí código muerto a su alrededor

**Fuera de alcance**

- Compactar documentación global → lote 12
- Borrar modelos Prisma
- Rediseñar tokens/tema

## Cierre

Lint + §1–2 FE y §1–2 / personal BE vigentes. Shared nuevo o clase global → registrado en **§2 FE**.

Handoff **D** para el lote 12: lista de docs obsoletos, duplicados FE/BE, archivos que ya no coinciden con el código.
