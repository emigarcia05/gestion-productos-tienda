---
description: Convenciones backend — Next.js 16, Prisma, Server Actions, servicios, Zod, permisos y tipos de respuesta
globs: src/actions/**/*.ts, src/services/**/*.ts, src/lib/**/*.ts, src/app/api/**/*.ts
alwaysApply: false
---

# Convenciones backend

Eres un agente experto en desarrollo backend. Escribe y modifica código simple, limpio y reutilizable, respetando siempre las tecnologías y convenciones de este proyecto.

## Tecnologías (respetar en todo momento)

- **Next.js 16** (App Router).
- **TypeScript** (tipado estricto).
- **Prisma** como ORM con PostgreSQL (Neon): cliente en `@/lib/prisma`. Para consultas raw o transacciones manuales usar el pool en `@/lib/db` (`query`, `getClient`).
- **Server Actions** en `src/actions/` con `"use server"`; exponen la lógica al frontend.
- **Capa de servicios** en `src/services/`: encapsulan acceso a datos y lógica de negocio; las actions llaman a los servicios, no a Prisma directamente en lógica compleja.
- **Validación con Zod**: schemas en `src/lib/validations/` (ej: `createProveedorSchema`). Usar `.safeParse()` y devolver errores de validación amigables.
- **Sesión y permisos**: `esEditor()` desde `@/lib/sesion` para proteger acciones que modifican datos.
- **Tipos de respuesta**: `ActionResult<T>` desde `@/lib/types` en actions (`{ ok, data? } | { ok: false, error: string }`); en servicios, `ServiceResult<T>` cuando aplique.

## Reglas de código

1. **Simple y legible**: funciones cortas, nombres claros, evita anidación innecesaria.
2. **Reutilizable**: extrae lógica repetida a funciones o servicios; no dupliques consultas ni validaciones.
3. **Estructura**: actions en `src/actions/`, servicios en `src/services/`, utilidades y DB en `src/lib/`. Rutas API en `src/app/api/` solo cuando haga falta un endpoint HTTP (webhooks, cron, etc.); preferir Server Actions para flujos desde la UI.
4. **Prisma**: usar el cliente de `@/lib/prisma` (singleton). Para transacciones, usar `getClient()` de `@/lib/db` si hace falta control explícito.
5. **Validación**: validar entradas con Zod en la action (o en el servicio si se reutiliza); mapear errores de Zod a mensajes claros en el `ActionResult`.
6. **Permisos**: en actions que crean/actualizan/eliminan, comprobar permisos (ej: `esEditor()`) al inicio y devolver `{ ok: false, error: "..." }` si no aplica.
7. **Cache**: usar `revalidatePath` (o `revalidateTag` si se usa) cuando se modifiquen datos que afecten a páginas concretas.
8. **Sin dependencias ajenas**: no introducir otros ORMs, otros sistemas de validación ni patrones ajenos al proyecto; mantener Prisma, Zod, Next.js y la estructura actual.

Al proponer o modificar código backend, aplica estas reglas y mantén la coherencia con el resto del código existente.
