---
description: Patrón de diseño global para nuevos módulos. Replicar exactamente esta estructura en cada módulo nuevo.
globs: **/app/**/page.tsx
---

# Patrón de módulos — Estructura obligatoria

Al crear un **nuevo módulo**, replicar exactamente esta estructura:

1. **Sidebar lateral**  
   El ítem del módulo debe existir en `Sidebar.tsx` (aside). La navegación es 100% desde el aside.

2. **Header de sección (Nivel 1)**  
   Usar siempre `SectionHeader` con:
   - `titulo`: h1 dinámico (ej. "Lista de Proveedores").
   - `descripcion`: línea en text-slate-500.
   - `actions` (opcional): acciones principales a la derecha del título (ej. Crear, Importar, Sincronizar).
   - `submoduleToolbar` (opcional): **línea horizontal de submódulos (Nivel 2)** debajo del título. Estilo Ghost/Pill Tabs: `variant="ghost"`, activo con `border-b-2 border-primary` y `bg-primary/5`. Separador sutil (`Separator`) entre header y contenido.

3. **Submódulos (Nivel 2) — Toolbar**  
   - No usar botones pesados: Ghost o Pill Tabs (`Button variant="ghost"`).
   - Activo: borde inferior 2px o fondo ligero #0072BB (`border-b-2 border-primary`, `bg-primary/5`).
   - **Acción principal** del módulo (ej. "Generar Pedido"): botón sólido #0072BB para destacar.
   - **Alerta** (ej. "Pedido Urgente"): icono Zap en #FFC107.
   - Otros: iconos Lucide en gris azulado (`text-slate-600`). Usar `flex gap-4` para alinear.
   - Al cambiar de módulo en la Sidebar, la toolbar de submódulos debe mostrar las opciones de esa sección.

4. **Barra de filtros**  
   Usar el componente `FilterBar` y los estilos Soft UI:
   - Input de búsqueda: `Input` de shadcn con icono `Search` (Lucide) al inicio; clases `INPUT_FILTER_CLASS` desde `@/components/FilterBar`.
   - Selects: `Select` de shadcn con `SelectTrigger` usando `SELECT_TRIGGER_FILTER_CLASS`; bordes `rounded-lg`.
   - Estética: fondo blanco (`bg-white`), borde gris sutil (`border-slate-200`), al foco borde #0072BB (`focus-visible:border-primary`).
   - Textos de filtros y contadores en `text-sm`.

5. **Contenido principal**  
   La tabla (o listado) va **dentro de un Card** de shadcn:
   - `rounded-xl`, `border-slate-200/60`, `shadow-sm`.
   - Fondo de página `bg-slate-50` para que el Card resalte.

6. **Interacción**  
   - Botones: hover suave (opacity o border/color).
   - Filas de tabla: `hover:bg-muted/50` y `transition-colors duration-150` (ya aplicado en `TableRow`).

Orden visual en la página:
`SectionHeader` (título + descripción + opcional submoduleToolbar) → `Separator` → `FilterBar` (filtros) → `Card` (tabla/contenido).
